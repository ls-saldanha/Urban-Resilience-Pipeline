
- [ ] **Geospatial Scope Definition:** Choose a metropolitan region for the initial mapping (e.g., "Metropolitan Area A").
- [ ] **Local Environment Setup:** Configure the Python environment with `requests`, `pydantic`, and GCP client tools.
- [ ] **Extraction Development:** Create the OpenAQ API collection script ensuring the handling of nested JSONs.
- [ ] **Simplified IaC:** Create the GCS buckets (`landing`, `processed`) and the BigQuery dataset via command line (`gsutil` / `bq`).
- [ ] **Business Logic (Transformation):** Develop the PySpark job to convert geographic coordinates into "Monitoring Zones."
- [ ] **Structured Versioning:** Organize Git with separate folders for `scripts/`, `spark_jobs/`, and `sql_models/`.
- [ ] **Strategic Posting:** Prepare a text highlighting the ability to handle "Social Network Analysis" (SNA) data adapted for sensors, as requested in job descriptions.